#!/usr/bin/env node
  const { print, factorial, Complex } = require("/home/usuario/practicas/practica_7/while-manuel-sebastian-noda-alu0101499918/src/support-lib.js"); 
  
/* End of support code */

 
  print(
    factorial(Complex("2")).pow(factorial(Complex("3")).pow(factorial(Complex("2"))))
);
  